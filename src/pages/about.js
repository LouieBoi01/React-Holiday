import React from "react";
import { useEffect } from 'react';

const About = () => {
    useEffect(() => {
        document.title = "Active Life | FAQ"
    });

    return (
        <div>
            <h1>
                Just getting a template ready.
            </h1>
        </div>
    );
};

export default About;